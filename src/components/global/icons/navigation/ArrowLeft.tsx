import { IoIosArrowBack } from "react-icons/io";
import { IconProps } from "@/types/icons";

const ArrowLeft = ({color, size}: IconProps) => {
    return ( <IoIosArrowBack color={color} size={size} />
 );
}
export default ArrowLeft;