import { IoIosArrowForward } from "react-icons/io";
import { IconProps } from "@/types/icons";

const ArrowRight = ({color, size}: IconProps) => {
    return ( <IoIosArrowForward color={color} size={size} />
 );
}
export default ArrowRight;